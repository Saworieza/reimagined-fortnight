<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete }, remote: true, class: "notification-icon") do |f| %>
  <%= f.submit "Unfollow", class: "cancel-request" %>
  <span class=" without-text fa-user-class"></span>
<% end %>


<%#= form_for(current_user.active_relationships.build, remote: true, class: "notification-icon") do |f| %>
  <!-- <div><%#= hidden_field_tag :followed_id, @user.id %></div> -->
  <%#= f.submit 'Follow', class: "accept-request fa fa-user-plus" do %>
  	<!-- <span class=" without-text fa-user-class"></span> -->
  <%# end %>
<%# end %>

<%#= form_for(current_user.active_relationships.find_by(followed_id: user.id), html: { method: :delete }, remote: true) do |f| %>
    <%#= f.submit "Unfollow", class: "btn-default" %>
<%# end %>