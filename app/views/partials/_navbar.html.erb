<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <nav class="main-nav">
        <%= link_to root_url, class: "" do %>
          <%= link_to image_tag('logo.png', class: "logo"), root_url %>
           <!-- <i class="fa fa-home"></i> MatatuGalore   -->
        <% end %>
        <% if user_signed_in? %>
<!--           <%#= link_to notifications_path, class: "dropdown-toggle", data_toggle: "dropdown", role: "button", aria_expanded: "false" do %>
            <i class="fa fa-bell-o ibadge"> 
              <!-- <span class='badge'> 
                <%#=  PublicActivity::Activity.order("created_at desc").where(owner_id: current_user.following_ids , owner_type: "User").unread_by(current_user).count %>
                <%#=  PublicActivity::Activity.unread_by(current_user).count %>
              </span> --
            </i> Notifications
          <%# end %> -->
          <%= link_to notifications_path, class: "" do %>
            <i class="fa fa-bell-o"></i> Notifications
          <% end %>

          <%= link_to chats_path, class: "" do %>
            <i class="fa fa-envelope-o"></i> Chats
          <% end %>
          <!-- <a class="dropdown">
            <a class="btn btn-primary dropdown-toggle" role="button" data-toggle="dropdown" aria-expanded="false">Dropdown Example
            <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <%# @activities.each do |activity| %>
              <li><a href="#"><%#=  PublicActivity::Activity.order("created_at desc").where(owner_id: current_user.following_ids , owner_type: "User").unread_by(current_user) %></a></li>
              <%# end %>
            </ul>
          </a>  -->
        <% end %>
      </nav>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
          <%= render 'partials/search' %>
          <li>
            <a href="#", class="user-avatar dropdown-toggle" data-toggle="dropdown", role="button", aria-expanded="false">
              <%= image_tag current_user.avatar.url || 'avatar.jpg', class: "avatar-xs", style: "width: 30px; height: 30px; border-radius: 50%;" %>
              <%= current_user.name %>
              <%= link_to "#{ current_user.verified ? '' : '' }", verified_user_path(current_user), method: :patch, remote: :true, style: "pointer-events: none; position: absolute !important; top: -5%;right: -8%;", class: "fa fa-#{ current_user.verified ? 'check-circle' : '' }"  %>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to "#{current_user.username}<br><b>@#{current_user.username}</b>".html_safe, user_path(current_user) %>
                </li>
              <li class="divider"></li>
              <li>
                <%= link_to "Settings", edit_user_registration_path %></li>
              <li><%= link_to('Sign out', destroy_user_session_path, method: :delete) %></li>
            </ul>
          </li>
          <% else %>
          <li>
             <a href="<%= new_user_session_path %>"><span style="color: #000;">Have an Account?</span> Sign In</a>
          </li>
          <!-- <form class="navbar-form navbar-right">
            <button id="new-tweet" type="button" data-toggle="modal" data-target="#new-tweet-modal" class="btn btn-primary"><i class="fa fa-paper-plane"></i> Tweet</button>
          </form> -->
          <% end %>
      </ul>
    </div>
  </div>
</nav>